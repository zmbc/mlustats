/* PlayerController test */

var HelloController = require('../../api/controllers/PlayerController'),  
    sinon = require('sinon'),
    should = require('should');

it('should work', function () {
	(true).should.equal(true);
});
